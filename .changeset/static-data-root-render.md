---
"@algoux/standard-ranklist-cli": patch
---

Add `srk render --static-data-root-url` for static directory previews that load JSON from an external URL root while preserving build-time SRK JSON validation.
